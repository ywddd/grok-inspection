name: CI

# Push / PR 后自动测试并编译多平台插件产物（无需本机安装 Go）
on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-build:
    name: Test & Build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            runner: ubuntu-latest
            ext: so
          - goos: windows
            goarch: amd64
            runner: windows-latest
            ext: dll
          - goos: darwin
            goarch: arm64
            # Pin to macos-15; macos-latest will move to macOS 26 in 2026.
            runner: macos-15
            ext: dylib

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.22.x"
          # Module has no external deps / no go.sum; disable cache to avoid warnings.
          cache: false

      - name: Install Linux C toolchain
        if: matrix.goos == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: Test
        shell: bash
        run: go test ./... -count=1

      - name: Build plugin (c-shared)
        shell: bash
        env:
          CGO_ENABLED: "1"
        run: |
          mkdir -p dist
          go build \
            -buildvcs=false \
            -buildmode=c-shared \
            -o "dist/grok-inspection.${{ matrix.ext }}" \
            .
          ls -la dist

      # GitHub 下载 artifact 时本身就会再打一层 zip。
      # 这里只上传插件本体，避免「解压后里面还有一个 zip」的套娃。
      - name: Upload plugin artifact
        uses: actions/upload-artifact@v7
        with:
          name: grok-inspection-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/grok-inspection.${{ matrix.ext }}
          if-no-files-found: error
          retention-days: 30

  freebsd-build:
    name: Test & Build (freebsd/amd64)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build in FreeBSD VM
        uses: vmactions/freebsd-vm@v1
        with:
          release: "14.3"
          usesh: true
          sync: rsync
          prepare: |
            pkg install -y go122 || pkg install -y go
          run: |
            set -eu
            GO="$(command -v go122 || command -v go)"
            "$GO" version
            "$GO" test ./... -count=1
            mkdir -p dist
            "$GO" build \
              -buildvcs=false \
              -buildmode=c-shared \
              -o "dist/grok-inspection.so" \
              .
            "$GO" version -m "dist/grok-inspection.so" | tee build-info.txt
            grep -q "GOOS=freebsd" build-info.txt
            grep -q "GOARCH=amd64" build-info.txt

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v7
        with:
          name: grok-inspection-freebsd-amd64
          path: dist/grok-inspection.so
          if-no-files-found: error
          retention-days: 30
